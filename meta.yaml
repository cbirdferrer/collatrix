package:
  name: MorphometrixCollating
  version: '1.0.0'

source:
  #path: morphometrix/
  git_url: https://github.com/cbirdferrer/MorphometrixCollating/tree/master/morphometrix_collating

build:
  noarch: python
  number: '0'
  script: python setup.py install #python -m pip install #--no-deps --ignore-installed  #

requirements:
  build:
    - python #setup.py install
 #   - setuptools
  run:
    - python
    - numpy
    - pandas
    - pyqt
    - os
    - math
    - setuptools

# tests:
#    imports:
#     - morphometrix

about:
  home: https://github.com/cbirdferrer/MorphometrixCollating
  license: MIT
  summary: A GUI to collate output of MorphoMetriX
